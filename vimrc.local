" General Configuration

syntax enable
set background=dark
colorscheme solarized
highlight clear SignColumn

let g:airline_powerline_fonts = 1

" Exit insert mode without using Esc
" Ctrl-[ seems more consistent (no unintended extra letters)
inoremap jk <Esc>
